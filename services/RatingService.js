class RatingService {

    addRating(addRatingRequest) {
        console.log(addRatingRequest);
        //TODO save the rating in the database and return the rating
    }
}

module.exports = RatingService;


